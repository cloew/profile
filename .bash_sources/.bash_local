#! /usr/bin/bash

# Source all local profiles

for file in .bash_kao .bash_excelcient .bash_odesk .bash_bluewolf
do
    if [ -f ~/.bash_sources/$file ]; then
        source ~/.bash_sources/$file
    fi
done