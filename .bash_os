#! /usr/bin/bash

case $OSTYPE in
    cygwin )
		if [ -f ~/.bash_cygwin ]; then
			source ~/.bash_cygwin
		fi ;;
    linux-gnu )
        if [ -f ~/.bash_linux ]; then
			source ~/.bash_linux
		fi ;;
esac